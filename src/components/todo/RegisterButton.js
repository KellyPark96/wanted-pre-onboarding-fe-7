import { TodoRegisterButton } from "./TodoContentsStyle"

const RegisterButton = () => {
    return (
        <TodoRegisterButton type="submit">Register</TodoRegisterButton>
    )
}

export default RegisterButton;
